name: Agent provider inventory

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  inventory:
    if: github.head_ref == 'agent/bpt-provider-api-v1'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inventory private Bayesian-PhysTwin imports
        run: |
          python - <<'PY'
          import ast
          import json
          from pathlib import Path

          records = []
          for root_name in ("src", "scripts", "tests"):
              root = Path(root_name)
              if not root.exists():
                  continue
              for path in sorted(root.rglob("*.py")):
                  try:
                      tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
                  except SyntaxError:
                      continue
                  for node in ast.walk(tree):
                      if not isinstance(node, ast.ImportFrom):
                          continue
                      if not (node.module or "").startswith("bayesian_phystwin."):
                          continue
                      private = [alias.name for alias in node.names if alias.name.startswith("_")]
                      if private:
                          records.append(
                              {
                                  "path": str(path),
                                  "line": node.lineno,
                                  "module": node.module,
                                  "names": private,
                              }
                          )
          print("BPT_PRIVATE_IMPORTS=" + json.dumps(records, sort_keys=True))
          print("BPT_PRIVATE_IMPORT_COUNT=" + str(sum(len(item["names"]) for item in records)))
          PY
      - name: Package source snapshot
        run: tar --exclude=.git -czf causal4d-source.tgz .
      - uses: actions/upload-artifact@v4
        with:
          name: causal4d-source
          path: causal4d-source.tgz
          retention-days: 1
