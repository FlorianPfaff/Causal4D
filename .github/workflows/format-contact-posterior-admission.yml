name: Format contact posterior admission

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/format-contact-posterior-admission.yml"
      - "scripts/ci/analyze_contact_posterior.py"
      - "src/causal4d/contact_posterior_admission.py"
      - "src/causal4d/contact_posterior_source_integrity.py"
      - "src/causal4d/result_bundle_verification.py"
      - "tests/test_contact_posterior_admission.py"
      - "tests/test_contact_posterior_bundle_verification.py"
      - "tests/test_contact_posterior_source_integrity.py"

permissions:
  contents: write

jobs:
  format:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out source branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: agent/reproducibility-contact-diagnostics
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Format diagnostic admission files
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install "ruff>=0.15.22,<0.17"
          python -m ruff format \
            scripts/ci/analyze_contact_posterior.py \
            src/causal4d/contact_posterior_admission.py \
            src/causal4d/contact_posterior_source_integrity.py \
            src/causal4d/result_bundle_verification.py \
            tests/test_contact_posterior_admission.py \
            tests/test_contact_posterior_bundle_verification.py \
            tests/test_contact_posterior_source_integrity.py
          python -m ruff check \
            scripts/ci/analyze_contact_posterior.py \
            src/causal4d/contact_posterior_admission.py \
            src/causal4d/contact_posterior_source_integrity.py \
            src/causal4d/result_bundle_verification.py \
            tests/test_contact_posterior_admission.py \
            tests/test_contact_posterior_bundle_verification.py \
            tests/test_contact_posterior_source_integrity.py

      - name: Commit formatting if needed
        shell: bash
        run: |
          set -euo pipefail
          git add \
            scripts/ci/analyze_contact_posterior.py \
            src/causal4d/contact_posterior_admission.py \
            src/causal4d/contact_posterior_source_integrity.py \
            src/causal4d/result_bundle_verification.py \
            tests/test_contact_posterior_admission.py \
            tests/test_contact_posterior_bundle_verification.py \
            tests/test_contact_posterior_source_integrity.py
          if git diff --cached --quiet; then
            echo "Formatting is already clean."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Format contact diagnostic admission"
          git push origin HEAD:agent/reproducibility-contact-diagnostics
