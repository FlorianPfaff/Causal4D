name: Temporary evaluation patch cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The one-time typing patch has already been applied."
