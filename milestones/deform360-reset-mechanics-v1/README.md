# Deform360 observed-reset mechanics result

This compact milestone binds the authoritative source-only workflow artifact from
run `30977227684`. The frozen prefix baseline reproduced on all 30 source episodes,
but eight registered reset constructions failed across seven rope episodes because
the maximum-neighbour filament graph remained disconnected. The preregistered
minimum of 24 complete episodes was therefore not met.

The result is classified as `insufficient_common_episode_support`. It does not
support a mechanics-location claim and does not authorize repairing, replacing, or
omitting the failed resets inside the opened study. On the 23 complete episodes,
the unchanged observed-reset simulator lost to persistence at every registered
horizon.

`summary.json` records the exact workflow, artifact, result, runtime, decision,
failed-reset, and target-closed identities. The next permitted structural study is
a separately versioned source-only filament-support boundary. It cannot revise the
opened reset result, the registered estimator, or the physical evidence count.
